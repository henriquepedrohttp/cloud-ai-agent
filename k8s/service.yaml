apiVersion: v1
kind: Service
metadata:
  name: ai-agent-service
  namespace: default
spec:
  selector:
    app: ai-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
